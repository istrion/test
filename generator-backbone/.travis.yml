language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-backbone' ]; then cd .. && eval "mv $currentfolder generator-backbone" && cd generator-backbone; fi

